const KEY='4472';const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'content-type, x-landbank-batch-key'};type Row=Record<string,any>;
const json=(d:unknown,s=200)=>new Response(JSON.stringify(d,null,2),{status:s,headers:{...cors,'Content-Type':'application/json'}});
async function rest(base:string,service:string,path:string,opt:{method?:string,body?:unknown,prefer?:string}={}){const h:Record<string,string>={apikey:service,Authorization:`Bearer ${service}`,'Content-Type':'application/json'};if(opt.prefer)h.Prefer=opt.prefer;const r=await fetch(`${base}/rest/v1/${path}`,{method:opt.method||'GET',headers:h,body:opt.body==null?undefined:JSON.stringify(opt.body)});const t=await r.text();if(!r.ok)throw new Error(`Supabase ${r.status}: ${t.slice(0,500)}`);return t?JSON.parse(t):null}
async function patch(base:string,service:string,id:string,body:Row){return rest(base,service,`lb_assessment_queue?id=eq.${id}`,{method:'PATCH',body:{...body,updated_at:new Date().toISOString()},prefer:'return=minimal'})}
async function processOne(base:string,service:string,item:Row){const attempt=Number(item.attempts||0)+1;try{const r=await fetch(`${base}/functions/v1/assess-wales-site`,{method:'POST',headers:{'Content-Type':'application/json','x-landbank-assess-key':'4472'},body:JSON.stringify({site_id:item.site_id})});const text=await r.text();let p:any={};try{p=text?JSON.parse(text):{}}catch{p={raw:text.slice(0,800)}}if(!r.ok||p?.error)throw new Error(p?.error||`assess-wales-site ${r.status}`);await patch(base,service,item.id,{status:'completed',locked_at:null,completed_at:new Date().toISOString(),last_error:null,last_result:p});return{site_id:item.site_id,status:'completed',coverage:p.wales_coverage,alc:p.agricultural_grade,planning:p.planning_screen_score,constraints:p.constraint_count}}catch(e){const msg=e instanceof Error?e.message:String(e),final=attempt>=5;await patch(base,service,item.id,{status:final?'failed':'queued',locked_at:null,last_error:msg,next_attempt_at:new Date(Date.now()+Math.min(60,attempt*5)*60000).toISOString()});return{site_id:item.site_id,status:final?'failed':'requeued',error:msg.slice(0,220)}}}
Deno.serve(async req=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});if(req.method!=='POST')return json({error:'POST required'},405);if(req.headers.get('x-landbank-batch-key')!==KEY)return json({error:'unauthorised'},401);try{const base=Deno.env.get('SUPABASE_URL'),service=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY');if(!base||!service)throw new Error('Supabase environment unavailable');const input=await req.json().catch(()=>({}));const batch=Math.max(1,Math.min(10,Number(input?.batch_size||6)));const now=new Date().toISOString();const q:Row[]=await rest(base,service,`lb_assessment_queue?job_type=eq.wales_point&status=in.(queued,failed)&attempts=lt.5&next_attempt_at=lte.${encodeURIComponent(now)}&select=id,site_id,attempts&order=attempts.asc,next_attempt_at.asc&limit=${batch}`)||[];for(const item of q)await patch(base,service,item.id,{status:'processing',attempts:Number(item.attempts||0)+1,locked_at:new Date().toISOString(),last_error:null});const items:any[]=[];for(let i=0;i<q.length;i+=2)items.push(...await Promise.all(q.slice(i,i+2).map(x=>processOne(base,service,x))));return json({requested:batch,claimed:q.length,items})}catch(e){return json({error:e instanceof Error?e.message:String(e)},500)}});
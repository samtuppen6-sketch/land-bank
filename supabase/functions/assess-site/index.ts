// LandBank V2 technical site assessment: PVGIS + lean origination planning point screen.
const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'content-type, x-landbank-assess-key'};const ASSESS_KEY='4472';type Row=Record<string,any>;
function json(data:unknown,status=200){return new Response(JSON.stringify(data,null,2),{status,headers:{...cors,'Content-Type':'application/json'}})}
function clamp(v:number,lo=0,hi=100){return Math.max(lo,Math.min(hi,v))}function round1(v:number){return Math.round(v*10)/10}
async function rest(base:string,service:string,path:string,opt:{method?:string,body?:unknown,prefer?:string}={}){const headers:Record<string,string>={apikey:service,Authorization:`Bearer ${service}`,'Content-Type':'application/json'};if(opt.prefer)headers.Prefer=opt.prefer;const r=await fetch(`${base}/rest/v1/${path}`,{method:opt.method||'GET',headers,body:opt.body==null?undefined:JSON.stringify(opt.body)});const text=await r.text();if(!r.ok)throw new Error(`Supabase ${r.status}: ${text.slice(0,600)}`);return text?JSON.parse(text):null}
async function getSite(base:string,service:string,siteId:string){const rows=await rest(base,service,`lb_sites?id=eq.${encodeURIComponent(siteId)}&select=*`);return rows?.[0]||null}
async function pvgis(lat:number,lon:number){const q=new URLSearchParams({lat:String(lat),lon:String(lon),peakpower:'1',loss:'14',mountingplace:'free',optimalinclination:'1',outputformat:'json'});const url=`https://re.jrc.ec.europa.eu/api/v5_3/PVcalc?${q}`;const r=await fetch(url,{headers:{'User-Agent':'LandBank-V2/1.0'}});const text=await r.text();if(!r.ok)throw new Error(`PVGIS ${r.status}: ${text.slice(0,500)}`);const body=JSON.parse(text),fixed=body?.outputs?.totals?.fixed||{},annual=Number(fixed.E_y||0);return{score:annual>0?round1(clamp((annual-750)/3.5)):null,annual_kwh_per_kwp:annual||null,annual_irradiation_kwh_m2:Number(fixed['H(i)_y']||0)||null,source_url:url,raw:body}}
const COVERAGE='local-authority-district';
const CONSTRAINTS=[COVERAGE,'green-belt','site-of-special-scientific-interest','special-protection-area','special-area-of-conservation','ramsar','ancient-woodland','scheduled-monument','area-of-outstanding-natural-beauty','national-park','national-nature-reserve','local-nature-reserve','battlefield','listed-building','conservation-area','heritage-coast'];
const PENALTY:Record<string,number>={'site-of-special-scientific-interest':30,'special-protection-area':30,'special-area-of-conservation':30,'ramsar':30,'ancient-woodland':25,'scheduled-monument':25,'area-of-outstanding-natural-beauty':20,'national-park':20,'national-nature-reserve':20,'battlefield':15,'listed-building':15,'green-belt':10,'conservation-area':10,'local-nature-reserve':10,'heritage-coast':10};
async function planning(lat:number,lon:number){const q=new URLSearchParams({latitude:String(lat),longitude:String(lon),limit:'100'});CONSTRAINTS.forEach(d=>q.append('dataset',d));for(const f of ['entity','name','dataset','reference'])q.append('field',f);const url=`https://www.planning.data.gov.uk/entity.json?${q}`;const r=await fetch(url,{headers:{'User-Agent':'LandBank-V2/1.0'}});const text=await r.text();if(!r.ok)throw new Error(`Planning Data ${r.status}: ${text.slice(0,500)}`);const body=JSON.parse(text),entities:Row[]=body?.entities||[],byDataset:Record<string,number>={};for(const e of entities)byDataset[e.dataset]=(byDataset[e.dataset]||0)+1;const england=Boolean(byDataset[COVERAGE]);const constraintDatasets=Object.keys(byDataset).filter(d=>d!==COVERAGE);const penalty=constraintDatasets.reduce((sum,d)=>sum+(PENALTY[d]||0),0);const constraints=entities.filter(e=>e.dataset!==COVERAGE).map(e=>({entity:e.entity,name:e.name,dataset:e.dataset,reference:e.reference}));return{score:england?round1(clamp(100-penalty)):null,england_coverage:england,constraint_count:constraints.length,datasets:Object.fromEntries(Object.entries(byDataset).filter(([k])=>k!==COVERAGE)),constraints,caveat:'Origination point pre-screen only. The farm pin may not represent the proposed solar field. Formal parcel-level planning/environment due diligence remains downstream.',source_url:url,raw:body}}
function availableWeighted(site:Row,overrides:Row={}){const c:Array<[unknown,number]>=[[overrides.grid_score??site.grid_score,25],[overrides.land_score??site.land_score,20],[overrides.planning_score??site.planning_score,15],[overrides.agricultural_score??site.agricultural_score,10],[overrides.topography_score??site.topography_score,10],[overrides.solar_score??site.solar_score,10],[overrides.ownership_score??site.ownership_score,10]];const v:Array<[number,number]>=c.filter(([raw])=>raw!==null&&raw!==undefined&&raw!==''&&Number.isFinite(Number(raw))).map(([raw,w])=>[Number(raw),w]);if(!v.length)return null;const weight=v.reduce((a,[,w])=>a+w,0);return round1(v.reduce((a,[n,w])=>a+n*w,0)/weight)}
async function upsertAssessment(base:string,service:string,row:Row){return rest(base,service,'lb_site_assessments?on_conflict=site_id,assessment_type,provider',{method:'POST',body:[row],prefer:'resolution=merge-duplicates,return=minimal'})}
Deno.serve(async req=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});if(req.method!=='POST')return json({error:'POST required'},405);if(req.headers.get('x-landbank-assess-key')!==ASSESS_KEY)return json({error:'unauthorised'},401);try{const input=await req.json(),siteId=String(input?.site_id||'');if(!siteId)return json({error:'site_id required'},400);const base=Deno.env.get('SUPABASE_URL'),service=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY');if(!base||!service)throw new Error('Supabase service environment unavailable');const site=await getSite(base,service,siteId);if(!site)return json({error:'site not found'},404);if(site.lat==null||site.lng==null)return json({error:'site has no coordinates'},422);const providers=Array.isArray(input?.providers)&&input.providers.length?input.providers:['pvgis','planning'];const now=new Date().toISOString(),result:Row={site_id:siteId,site_name:site.name,lat:site.lat,lng:site.lng,assessed_at:now},patch:Row={updated_at:now};if(providers.includes('pvgis')){try{const x=await pvgis(Number(site.lat),Number(site.lng));result.pvgis={score:x.score,annual_kwh_per_kwp:x.annual_kwh_per_kwp,annual_irradiation_kwh_m2:x.annual_irradiation_kwh_m2};patch.solar_score=x.score;await upsertAssessment(base,service,{site_id:siteId,assessment_type:'solar',score:x.score,status:'screened',raw_data:x.raw,summary:{annual_kwh_per_kwp:x.annual_kwh_per_kwp,annual_irradiation_kwh_m2:x.annual_irradiation_kwh_m2},provider:'PVGIS 5.3',source_ref:x.source_url,assessed_at:now})}catch(e){result.pvgis={error:e instanceof Error?e.message:String(e)}}}if(providers.includes('planning')){try{const x=await planning(Number(site.lat),Number(site.lng));result.planning={score:x.score,constraint_count:x.constraint_count,datasets:x.datasets,caveat:x.caveat,provisional:true,england_coverage:x.england_coverage};patch.planning_screen_score=x.score;patch.planning_score_confidence=x.england_coverage?45:0;patch.planning_screened_at=now;patch.planning_evidence_class=x.england_coverage?'planning_data_point_screen':'outside_england_or_unknown';patch.planning_constraint_count=x.constraint_count;await upsertAssessment(base,service,{site_id:siteId,assessment_type:'planning',score:x.score,status:x.england_coverage?'provisional_point_screen':'outside_england_or_unknown',raw_data:x.raw,summary:{constraint_count:x.constraint_count,datasets:x.datasets,constraints:x.constraints,caveat:x.caveat,provisional:true,england_coverage:x.england_coverage},provider:'Planning Data API',source_ref:x.source_url,assessed_at:now})}catch(e){result.planning={error:e instanceof Error?e.message:String(e)}}}patch.site_score=availableWeighted(site,patch);await rest(base,service,`lb_sites?id=eq.${encodeURIComponent(siteId)}`,{method:'PATCH',body:patch,prefer:'return=minimal'});result.site_score=patch.site_score;return json(result)}catch(e){return json({error:e instanceof Error?e.message:String(e)},500)}});
const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type'};
type Input={company_number?:string;company_name?:string;town?:string;postcode?:string;director_name?:string;domain?:string;providers?:Array<'companies_house'|'google_places'|'hunter'>};
type Contact={type:'phone'|'email'|'website';value:string;provider:string;verification_status:'verified'|'probable'|'catch_all'|'unknown'|'invalid';confidence:number;source_ref?:string;discovery_method:string};
const json=(d:unknown,s=200)=>new Response(JSON.stringify(d),{status:s,headers:{...cors,'Content-Type':'application/json'}});
const cleanName=(s='')=>s.toUpperCase().replace(/\b(LIMITED|LTD|LLP|PLC|FARMS?|FARMING|AGRICULTURE|AGRICULTURAL|HOLDINGS?)\b/g,' ').replace(/[^A-Z0-9 ]/g,' ').replace(/\s+/g,' ').trim();
const normPc=(s='')=>s.toUpperCase().replace(/\s/g,'');
const outwardPc=(s='')=>normPc(s).slice(0,-3);
const domainOnly=(s='')=>s.trim().toLowerCase().replace(/^https?:\/\//,'').replace(/^www\./,'').split('/')[0];
const nameTokens=(s='')=>new Set(cleanName(s).split(' ').filter(x=>x.length>=3));
function tokenOverlap(a='',b=''){const aa=nameTokens(a),bb=nameTokens(b);if(!aa.size||!bb.size)return 0;let hit=0;aa.forEach(t=>{if(bb.has(t))hit++});return hit/Math.max(aa.size,bb.size)}
function splitPerson(full=''){const p=full.trim().split(/\s+/).filter(Boolean);return p.length<2?{first_name:'',last_name:''}:{first_name:p[0],last_name:p[p.length-1]}}
function chHeaders(raw:string){let x=raw.trim();if((x.startsWith('"')&&x.endsWith('"'))||(x.startsWith("'")&&x.endsWith("'")))x=x.slice(1,-1).trim();if(/^basic\s+/i.test(x))return [{Authorization:x,mode:'prebuilt'}];return [{Authorization:'Basic '+btoa(x+':'),mode:'raw_key'},{Authorization:'Basic '+x,mode:'encoded_fallback'}]}
async function companiesHouse(input:Input){const raw=Deno.env.get('COMPANIES_HOUSE_API_KEY');if(!raw)return{provider:'companies_house',skipped:true,reason:'missing_api_key'};if(!input.company_number)return{provider:'companies_house',skipped:true,reason:'missing_company_number'};const base='https://api.company-information.service.gov.uk',no=encodeURIComponent(input.company_number);let companyRes:Response|null=null,officerRes:Response|null=null,auth_mode='';for(const h of chHeaders(raw)){const [a,b]=await Promise.all([fetch(`${base}/company/${no}`,{headers:{Authorization:h.Authorization}}),fetch(`${base}/company/${no}/officers?items_per_page=35`,{headers:{Authorization:h.Authorization}})]);companyRes=a;officerRes=b;auth_mode=h.mode;if(a.ok||b.ok||![400,401].includes(a.status))break}const company=companyRes?.ok?await companyRes.json():null;const officerBody=officerRes?.ok?await officerRes.json():{items:[]};const officers=(officerBody.items||[]).filter((x:any)=>!x.resigned_on&&x.officer_role!=='corporate-director').map((x:any)=>({name:x.name,role:x.officer_role,occupation:x.occupation||'',appointed_on:x.appointed_on||null,address:x.address||null,farming_signal:/farm|agricult|land/i.test(`${x.occupation||''} ${x.name||''}`)})).sort((a:any,b:any)=>Number(b.farming_signal)-Number(a.farming_signal)||String(a.appointed_on||'').localeCompare(String(b.appointed_on||'')));return{provider:'companies_house',ok:!!(companyRes?.ok||officerRes?.ok),status:companyRes?.status||officerRes?.status||null,auth_mode,company:company?{company_number:company.company_number,company_name:company.company_name,company_status:company.company_status,sic_codes:company.sic_codes||[],registered_office_address:company.registered_office_address||null,has_charges:company.has_charges??null,has_insolvency_history:company.has_insolvency_history??null}:null,officers,primary_director:officers[0]||null}}
async function googlePlaces(input:Input){const key=Deno.env.get('GOOGLE_PLACES_API_KEY');if(!key)return{provider:'google_places',skipped:true,reason:'missing_api_key'};if(!input.company_name)return{provider:'google_places',skipped:true,reason:'missing_company_name'};const query=[input.company_name,input.town,input.postcode].filter(Boolean).join(' ');const res=await fetch('https://places.googleapis.com/v1/places:searchText',{method:'POST',headers:{'Content-Type':'application/json','X-Goog-Api-Key':key,'X-Goog-FieldMask':'places.id,places.displayName,places.formattedAddress,places.nationalPhoneNumber,places.websiteUri,places.primaryType'},body:JSON.stringify({textQuery:query,maxResultCount:5})});if(!res.ok)return{provider:'google_places',ok:false,status:res.status};const body=await res.json(),targetPc=normPc(input.postcode||''),targetOut=outwardPc(input.postcode||'');const candidates=(body.places||[]).map((p:any)=>{const addr=p.formattedAddress||'',addrPc=normPc(addr),exactPc=!!targetPc&&addrPc.includes(targetPc),outward=!!targetOut&&addrPc.includes(targetOut),overlap=tokenOverlap(input.company_name||'',p.displayName?.text||''),match_score=Math.min(100,Math.round((exactPc?55:outward?30:0)+overlap*40+(p.websiteUri?5:0)));return{place_id:p.id,name:p.displayName?.text||'',formatted_address:addr,phone:p.nationalPhoneNumber||'',website:p.websiteUri||'',primary_type:p.primaryType||'',match_score,postcode_exact:exactPc,name_overlap:Math.round(overlap*100)}}).sort((a:any,b:any)=>b.match_score-a.match_score);const best=candidates[0]||null,contacts:Contact[]=[];if(best&&best.match_score>=80){if(best.phone)contacts.push({type:'phone',value:best.phone,provider:'google_places',verification_status:'probable',confidence:best.match_score,source_ref:best.place_id,discovery_method:'google_places_text_search'});if(best.website)contacts.push({type:'website',value:best.website,provider:'google_places',verification_status:'probable',confidence:best.match_score,source_ref:best.place_id,discovery_method:'google_places_text_search'})}return{provider:'google_places',ok:true,query,best,candidates,contacts,trusted:!!best&&best.match_score>=80}}
async function hunter(input:Input){const key=Deno.env.get('HUNTER_API_KEY');if(!key)return{provider:'hunter',skipped:true,reason:'missing_api_key'};const supplied=domainOnly(input.domain||'');if(!supplied&&!input.company_name)return{provider:'hunter',skipped:true,reason:'missing_domain_or_company'};const root='https://api.hunter.io/v2',domainQs=supplied?`domain=${encodeURIComponent(supplied)}`:`company=${encodeURIComponent(input.company_name||'')}`,domainRes=await fetch(`${root}/domain-search?${domainQs}&limit=10&api_key=${encodeURIComponent(key)}`),domainBody=domainRes.ok?await domainRes.json():null,resolvedDomain=supplied||domainBody?.data?.domain||'',org=domainBody?.data?.organization||'',orgOverlap=supplied?1:tokenOverlap(input.company_name||'',org),domainTrusted=!!resolvedDomain&&(!!supplied||orgOverlap>=0.5);let finderBody:any=null;if(input.director_name&&domainTrusted){const p=splitPerson(input.director_name);if(p.first_name&&p.last_name){const fr=await fetch(`${root}/email-finder?domain=${encodeURIComponent(resolvedDomain)}&first_name=${encodeURIComponent(p.first_name)}&last_name=${encodeURIComponent(p.last_name)}&api_key=${encodeURIComponent(key)}`);if(fr.ok)finderBody=await fr.json()}}const contacts:Contact[]=[],named=finderBody?.data?.email,ver=finderBody?.data?.verification?.status||'';if(named&&domainTrusted)contacts.push({type:'email',value:named,provider:'hunter',verification_status:ver==='valid'?'verified':ver==='accept_all'?'catch_all':'unknown',confidence:Math.round(Number(finderBody?.data?.score||0)),source_ref:(finderBody?.data?.sources||[])[0]?.uri,discovery_method:'hunter_email_finder'});const published=(domainBody?.data?.emails||[]).slice(0,10).map((e:any)=>({email:e.value,type:e.type,confidence:Number(e.confidence||0),first_name:e.first_name,last_name:e.last_name,position:e.position,verification:e.verification?.status||null,sources:e.sources||[]}));if(domainTrusted){const eligible=published.filter((e:any)=>e.email&&e.email!==named&&(e.verification==='valid'||(e.verification==='accept_all'&&e.confidence>=90))).sort((a:any,b:any)=>(b.verification==='valid'?1:0)-(a.verification==='valid'?1:0)||b.confidence-a.confidence).slice(0,3);for(const e of eligible)contacts.push({type:'email',value:e.email,provider:'hunter',verification_status:e.verification==='valid'?'verified':'catch_all',confidence:e.confidence,source_ref:e.sources?.[0]?.uri,discovery_method:'hunter_domain_search'})}return{provider:'hunter',ok:domainRes.ok,domain:resolvedDomain,domain_trusted:domainTrusted,organization:org,organization_overlap:Math.round(orgOverlap*100),pattern:domainBody?.data?.pattern||null,accept_all:domainBody?.data?.accept_all??null,published_emails:published,named_email:finderBody?.data||null,contacts}}
Deno.serve(async req=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});if(req.method!=='POST')return json({error:'POST required'},405);try{const input=await req.json() as Input,requested=new Set(input.providers?.length?input.providers:['companies_house','google_places','hunter']),results:any[]=[],working={...input};if(requested.has('companies_house')){const ch:any=await companiesHouse(working);results.push(ch);if(!working.director_name&&ch?.primary_director?.name)working.director_name=ch.primary_director.name}if(requested.has('google_places')){const gp:any=await googlePlaces(working);results.push(gp);if(!working.domain&&gp?.trusted&&gp?.best?.website)working.domain=domainOnly(gp.best.website)}if(requested.has('hunter'))results.push(await hunter(working));const contacts=results.flatMap((r:any)=>r.contacts||[]);return json({input,resolved:{director_name:working.director_name||null,domain:working.domain||null},contacts,providers:results,human_review_required:results.some((r:any)=>(r.provider==='google_places'&&r.best&&!r.trusted)||(r.provider==='hunter'&&r.domain&&!r.domain_trusted))})}catch(e){return json({error:e instanceof Error?e.message:String(e)},500)}});